apiVersion: v1
kind: Service
metadata:
  name: {{ include "vulnops-hub.fullname" . }}
  labels:
    {{- include "vulnops-hub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vulnops-hub.selectorLabels" . | nindent 4 }}
